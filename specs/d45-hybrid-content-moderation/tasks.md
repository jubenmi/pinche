# D45 Tasks：微信文本/图片与腾讯云视频混合内容审核

更新日期：2026-07-12

> 本清单替代旧“腾讯云全内容审核”路线。此前实现只作为待清理代码，不代表新路线任务完成；除三件套外全部重新按新需求验证后勾选。

## D45 执行任务

- [x] D45.1 重写中文 D45 spec 三件套。
  - [x] requirements 明确微信文本/图片、腾讯云视频、私有 COS 与 API 门禁。
  - [x] design 明确双服务商适配、状态机、回调、重试、后台和发布设计。
  - [x] tasks 重置为新路线的 Kiro 风格执行清单。

- [ ] D45.2 清理旧腾讯云全内容审核方案。
  - [ ] 先增加静态检查，证明 TMS、CI 图片策略和 COS 自动审核配置仍存在时失败。
  - [ ] 删除腾讯云 TMS 文本客户端、调用和配置。
  - [ ] 删除腾讯云 CI 图片审核客户端、调用和配置。
  - [ ] 删除 COS 图片自动审核/全桶冻结依赖与文档。
  - [ ] 保留统一数据模型、状态机、读取门禁和腾讯云视频代码。
  - [ ] 将通用命名与 provider 值迁移为混合方案。

- [ ] D45.3 校准审核数据模型和状态机。
  - [ ] 测试微信 trace_id、腾讯云视频 JobId、不可变版本和 provider 唯一约束。
  - [ ] 保留历史媒体 `approved_legacy`，新媒体显式 pending。
  - [ ] 文本 Review/Error 使用隐藏提案。
  - [ ] 管理员决定优先于服务商事件。
  - [ ] 重复和过期结果幂等且不覆盖有效终态。

- [ ] D45.4 实现微信 access token 管理与客户端。
  - [ ] 先测试 token 缓存、提前刷新、并发单飞和失效后单次重试。
  - [ ] 实现服务端 AppID/AppSecret 配置与生产关闭式校验。
  - [ ] 实现 `msgSecCheck` 客户端。
  - [ ] 实现 `mediaCheckAsync(media_type=2)` 客户端。
  - [ ] 确认日志和错误摘要不含密钥、token、完整文本或签名 URL。

- [ ] D45.5 接入微信文本审核。
  - [ ] 先覆盖昵称、评价、留言、置顶留言、私有门店/剧本和拼车说明的四类结果测试。
  - [ ] 多字段按稳定标签合并，并传生产者 openid 与正确 scene。
  - [ ] Pass 应用业务变更。
  - [ ] Review/Error 保存隐藏提案且不修改实体。
  - [ ] Block 返回稳定错误且不修改实体。
  - [ ] 排除系统文本、手机号和管理员审核备注。

- [ ] D45.6 接入微信异步图片审核。
  - [ ] 图片 finalize 在事务内创建 pending 媒体和微信任务。
  - [ ] 只生成供微信抓取的短时 COS URL，不返回用户。
  - [ ] 提交 `mediaCheckAsync` 并保存 trace_id。
  - [ ] 提交失败保持隐藏并进入重试。
  - [ ] Pass 批准；Review 入后台；Block 隐藏并幂等清理。

- [ ] D45.7 实现微信图片审核事件接收。
  - [ ] 测试错误签名、超大请求、未知结构、合法、重复和过期事件。
  - [ ] 验证微信签名；安全模式下验签并解密。
  - [ ] 匹配 trace_id、媒体 ID、对象 Key 和 ETag。
  - [ ] 使用事务行锁执行状态迁移。
  - [ ] 管理员决定后的事件不得覆盖当前状态。

- [ ] D45.8 收窄并验证腾讯云视频审核。
  - [ ] 客户端只暴露视频画面与音轨审核方法。
  - [ ] 源对象稳定后按媒体 ID 与 ETag 幂等提交。
  - [ ] 转码 ready 不修改内容审核状态。
  - [ ] 回调验证任务、data_id、对象 Key 与 ETag。
  - [ ] Block 幂等清理源、展示和封面对象。

- [ ] D45.9 封闭全部媒体读取路径。
  - [ ] 普通列表、公开分享、计数、标签和批量下载只包含 approved/approved_legacy。
  - [ ] 图片预览、缩略图、下载独立校验。
  - [ ] 视频 range、封面、播放和下载独立校验。
  - [ ] 只有 approved/approved_legacy 生成短时签名 URL。
  - [ ] 上传者只收到无 URL 状态占位。

- [ ] D45.10 实现混合审核重试 Worker。
  - [ ] 使用 `FOR UPDATE SKIP LOCKED` 和租约令牌。
  - [ ] 网络、超时、限流和 5xx 使用有限指数退避与抖动。
  - [ ] 微信 token/权限/额度和腾讯云 CAM/策略/额度/欠费保持隐藏并告警。
  - [ ] 重试耗尽后保持 error 并停止自动认领。
  - [ ] 提供生产 Worker 命令与运行配置。

- [ ] D45.11 实现管理员审核 API。
  - [ ] 普通用户 403；队列支持 provider、类型、状态、标签和时间筛选。
  - [ ] 提供详情、approve、reject、retry。
  - [ ] reject 必填原因，所有动作写审计。
  - [ ] 文本批准原子校验基线，变化时 stale。
  - [ ] retry 不改变内容可见性。

- [ ] D45.12 实现管理后台最小复核页面。
  - [ ] 增加内容审核导航、筛选队列、详情和操作。
  - [ ] 展示服务商、提交者、关联实体、建议、标签、重试和时间。
  - [ ] 媒体使用管理员短时预览 URL。
  - [ ] 运行后台测试与构建。

- [ ] D45.13 改造小程序审核体验。
  - [ ] pending/error、review、rejected 使用规定中文文案。
  - [ ] 文本 Review 不更新页面当前值。
  - [ ] 不展示服务商标签、分数或敏感命中词。
  - [ ] 运行小程序测试与构建。

- [ ] D45.14 增加指标、告警与孤儿对象扫描。
  - [ ] 指标覆盖创建、提交、延迟、结果、重试和未批准访问。
  - [ ] 告警覆盖微信 token/权限/额度和腾讯云视频 CAM/额度/欠费。
  - [ ] 监控队列深度、最老年龄、回调认证失败和重试耗尽。
  - [ ] 扫描 COS 对象、媒体记录和审核任务不一致。
  - [ ] 日志与指标不包含敏感凭证、完整私密文本或签名 URL。

- [ ] D45.15 编写混合方案生产手册。
  - [ ] 微信内容安全权限、access token、事件推送和调用额度。
  - [ ] 腾讯云 CI 视频画面/音轨策略、CAM、费用和欠费告警。
  - [ ] COS 私有桶、上传权限和短时签名 URL。
  - [ ] 分阶段启用、观察指标与安全回滚。
  - [ ] 明确不启用腾讯云 TMS、CI 图片和 COS 图片自动审核。

- [ ] D45.16 增加 D45 自动检查与烟测。
  - [ ] `d45:unit` 覆盖微信 token/客户端、映射、状态机、双回调和重试。
  - [ ] `d45:check` 覆盖迁移、入口、配置清理和读取门禁。
  - [ ] `d45:smoke` 覆盖微信文本、微信图片、腾讯云视频和人工复核主链路。
  - [ ] 加入根 `npm run check`，本地测试不依赖公网或生产凭证。

- [ ] D45.17 执行本地完整验证。
  - [ ] 运行 D45 unit/check/smoke。
  - [ ] 运行相册图片和视频现有测试。
  - [ ] 运行管理后台测试与构建。
  - [ ] 运行小程序媒体测试与构建。
  - [ ] 运行数据库迁移检查和根 `npm run check`。

- [ ] D45.18 执行非生产真实联调。
  - [ ] 微信文本 Pass/Review/Block/Error。
  - [ ] 微信图片提交、异步事件、重复和过期事件。
  - [ ] 腾讯云视频 Pass/Review/Block/Error。
  - [ ] 模拟 token、权限、超时和额度错误，确认内容保持隐藏并告警。
  - [ ] 记录延迟、策略和结论，不记录敏感样本正文或可复用 URL。

## D45 验收

- [x] 中文 requirements、design、tasks 已按混合方案重写。
- [ ] 腾讯云 TMS、CI 图片及 COS 图片自动审核依赖已清理。
- [ ] 微信文本与图片审核生效且异常不放行。
- [ ] 腾讯云只审核完整视频画面与音轨。
- [ ] 私有 COS 与全部 API 发布门禁阻止未批准内容访问。
- [ ] 管理员可复核并保留审计，小程序状态提示正确。
- [ ] 本地自动化验证全部通过。
- [ ] 非生产微信与腾讯云真实联调全部通过。

## 验证记录

- 2026-07-12：按用户确认路线重写三件套；尚未清理或继续实现业务代码，D45.2 起保持未完成。
