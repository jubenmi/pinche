# D39 Tasks: 同城车局只读预览执行清单

更新日期：2026-07-10

## D39 执行任务

- [x] D39.1 建立 spec 三件套。
  - [x] requirements 定义同城只读边界和分享卡上车边界。
  - [x] design 定义 `entry=city`、渲染控制、事件防护和测试策略。
  - [x] tasks 建立执行与验收清单。

- [ ] D39.2 建立 RED 自动检查。
  - [ ] 新增 `scripts/d39-city-preview-readonly-check.js`。
  - [ ] 检查同城详情 URL 包含 `entry=city`。
  - [ ] 检查只读提示、动作隐藏、事件早退和系统分享关闭。
  - [ ] 检查分享页上车协议保持不变。
  - [ ] 接入 `npm run check`。
  - [ ] 实际运行并确认因功能尚未实现而失败。

- [ ] D39.3 实现同城入口和详情只读状态。
  - [ ] 同城车卡跳转增加 `entry=city`。
  - [ ] 详情页读取入口并计算 `isCityPreview`。
  - [ ] 同城详情显示只读提示。
  - [ ] 同城详情关闭页面内和系统分享。

- [ ] D39.4 关闭同城详情中的成员动作。
  - [ ] 隐藏顶部动作区和记录编辑入口。
  - [ ] 座位和 NPC 卡片不生成动作。
  - [ ] 座位和 NPC 点击处理增加只读早退。
  - [ ] 不渲染聊天扩展。
  - [ ] 不调用成员关系恢复接口。
  - [ ] 保留基础信息、地图、座位状态和车友记录浏览。

- [ ] D39.5 自动验证。
  - [ ] D39 检查通过。
  - [ ] `node scripts/check-miniprogram.js` 通过。
  - [ ] `npm run check` 通过。
  - [ ] `npm run build:mp-weixin` 通过。
  - [ ] `git diff --check` 通过。

- [ ] D39.6 微信开发者工具验证。
  - [ ] 从同城车卡进入时显示只读提示。
  - [ ] 页面无选择、分享、管理、记录或聊天动作。
  - [ ] 座位和 NPC 点击无跳转。
  - [ ] 右上角系统分享不可用。
  - [ ] 通过现有分享卡片进入时仍可选择角色上车。

## D39 验收

- [ ] 同城详情只展示公开信息和状态。
- [ ] 同城详情不能上车、分享、管理、写记录或进入聊天。
- [ ] 同城详情不恢复成员关系。
- [ ] 店家或车友分享卡片仍可上车。
- [ ] 自动检查和小程序生产构建通过。

## 验证记录

- 2026-07-10：用户确认同城详情不保留分享按钮，采用 `entry=city` 的现有详情页只读方案；服务端签名邀请凭证不在本期范围内。
