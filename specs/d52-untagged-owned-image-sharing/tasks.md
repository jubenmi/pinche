# D52 本人未标注图片便捷分享任务

- [x] 1. 建立 D52 规格、计划和隔离基线
  - [x] 1.1 创建独立工作树并验证 D50 聚焦基线 35/35 通过。
  - [x] 1.2 创建 `requirements.md`、`design.md` 和 `tasks.md`。
  - [x] 1.3 写入并自检详细实施计划。

- [x] 2. 增加快照与标签版本数据契约
  - [x] 2.1 先写迁移和摘要兼容失败测试。
  - [x] 2.2 新增 `0033_album_untagged_share_preview.sql`。
  - [x] 2.3 实现隐式未标注条目规范化及兼容摘要。
  - [x] 2.4 验证旧快照摘要不变、新快照摘要覆盖标签版本。

- [x] 3. 放开本人未标注图片服务端资格
  - [x] 3.1 先写本人可分享、他人/视频/隐私不可分享的失败测试。
  - [x] 3.2 扩展统一公开资格与四级排序。
  - [x] 3.3 生成并持久化 `implicit_untagged_media`。
  - [x] 3.4 返回 `implicit_untagged_count`。

- [ ] 4. 动态读取与标签版本失效
  - [ ] 4.1 先写标签版本不匹配和公开字节读取失败测试。
  - [ ] 4.2 标签保存事务递增 `tag_version`。
  - [ ] 4.3 公开列表、图片、视频封面和播放统一传入快照上下文。
  - [ ] 4.4 验证未标注图片不进入封面。

- [ ] 5. 自定义分享范围 API
  - [ ] 5.1 先写自定义范围、超限和状态变化失败测试。
  - [ ] 5.2 扩展 share-token 路由请求参数。
  - [ ] 5.3 实现自定义范围严格校验，不静默补入媒体。
  - [ ] 5.4 验证旧客户端无 include 标志时保持旧行为。

- [ ] 6. 小程序分享预览纯状态
  - [ ] 6.1 先写预览路由、计数文案和选择限制失败测试。
  - [ ] 6.2 新增 `albumSharePreview.js` 最小实现。
  - [ ] 6.3 验证无 token、无效计数和数量超限均关闭式处理。

- [ ] 7. 小程序整册预览并分享
  - [ ] 7.1 先写 `album.vue` 契约失败测试。
  - [ ] 7.2 成员页增加“预览并分享”，停止页面加载时自动准备整册 token。
  - [ ] 7.3 分享预览展示准确提示、调整入口和原生分享按钮。
  - [ ] 7.4 成员页隐藏直接整册菜单，预览页 ready 后开启。

- [ ] 8. 小程序调整范围与单图分享
  - [ ] 8.1 先写 share 选择状态和单图 include 标志失败测试。
  - [ ] 8.2 复用多选 UI 增加独立 `share` purpose 和数量限制。
  - [ ] 8.3 保存选择后创建新快照并返回分享预览。
  - [ ] 8.4 单图请求显式允许本人未标注图片并显示提示。

- [ ] 9. 聚焦回归与规格收口
  - [ ] 9.1 运行 D52 API、小程序和迁移聚焦测试。
  - [ ] 9.2 运行 D48/D50 回归、语法检查和 `git diff --check`。
  - [ ] 9.3 逐条核对 requirements、design 和 tasks，记录自动验证证据。
  - [ ] 9.4 在微信开发者工具验证整册、调整、单图和朋友圈流程；未执行时保持未勾选并写明原因。
