# D52 本人未标注图片便捷分享任务

- [x] 1. 建立 D52 规格、计划和隔离基线
  - [x] 1.1 创建独立工作树并验证 D50 聚焦基线 35/35 通过。
  - [x] 1.2 创建 `requirements.md`、`design.md` 和 `tasks.md`。
  - [x] 1.3 写入并自检详细实施计划。

- [x] 2. 增加快照与标签版本数据契约
  - [x] 2.1 先写迁移和摘要兼容失败测试。
  - [x] 2.2 新增 `0033_album_untagged_share_preview.sql`。
  - [x] 2.3 实现隐式未标注条目规范化及兼容摘要。
  - [x] 2.4 验证旧快照摘要不变、新快照摘要覆盖标签版本。

- [x] 3. 放开本人未标注图片服务端资格
  - [x] 3.1 先写本人可分享、他人/视频/隐私不可分享的失败测试。
  - [x] 3.2 扩展统一公开资格与四级排序。
  - [x] 3.3 生成并持久化 `implicit_untagged_media`。
  - [x] 3.4 返回 `implicit_untagged_count`。

- [x] 4. 动态读取与标签版本失效
  - [x] 4.1 先写标签版本不匹配和公开字节读取失败测试。
  - [x] 4.2 标签保存事务递增 `tag_version`。
  - [x] 4.3 公开列表、图片、视频封面和播放统一传入快照上下文。
  - [x] 4.4 验证未标注图片不进入封面。

- [x] 5. 自定义分享范围 API
  - [x] 5.1 先写自定义范围、超限和状态变化失败测试。
  - [x] 5.2 扩展 share-token 路由请求参数。
  - [x] 5.3 实现自定义范围严格校验，不静默补入媒体。
  - [x] 5.4 验证旧客户端无 include 标志时保持旧行为。

- [x] 6. 小程序分享预览纯状态
  - [x] 6.1 先写预览路由、计数文案和选择限制失败测试。
  - [x] 6.2 新增 `albumSharePreview.js` 最小实现。
  - [x] 6.3 验证无 token、无效计数和数量超限均关闭式处理。

- [x] 7. 小程序整册预览并分享
  - [x] 7.1 先写 `album.vue` 契约失败测试。
  - [x] 7.2 成员页增加“预览并分享”，停止页面加载时自动准备整册 token。
  - [x] 7.3 分享预览展示准确提示、调整入口和原生分享按钮。
  - [x] 7.4 成员页隐藏直接整册菜单，预览页 ready 后开启。

- [x] 8. 小程序调整范围与单图分享
  - [x] 8.1 先写 share 选择状态和单图 include 标志失败测试。
  - [x] 8.2 复用多选 UI 增加独立 `share` purpose 和数量限制。
  - [x] 8.3 保存选择后创建新快照并返回分享预览。
  - [x] 8.4 单图请求显式允许本人未标注图片并显示提示。

- [ ] 9. 聚焦回归与规格收口
  - [x] 9.1 运行 D52 API、小程序和迁移聚焦测试。
  - [x] 9.2 运行 D48/D50 回归、语法检查和 `git diff --check`。
  - [x] 9.3 逐条核对 requirements、design 和 tasks，记录自动验证证据。
  - [ ] 9.4 在微信开发者工具验证整册、调整、单图和朋友圈流程；当前环境未执行真实微信分享，保留待人工验收。

## 自动验证证据（2026-07-22）

- `node --check`：API service、server 和小程序预览 helper 全部通过。
- D52/D50 聚焦测试：59 项通过，0 项失败；覆盖迁移、摘要兼容、本人/他人资格、隐私否决、标签版本失效、公开图片字节复核、自定义范围、预览路由、菜单门禁、范围调整和单图分享。
- `node scripts/d48-album-sharing-role-claim-separation-check.js`：通过。
- `node scripts/d50-album-single-media-sharing-check.js`：通过。
- `npm run build:mp-weixin --workspace @pinche/miniprogram`：构建成功；仅有既有 Sass deprecation warning。
- `git diff --check`：通过。
- Requirements 1–8 均有实现与自动测试证据；Requirement 4/5/6 的微信好友、群和朋友圈真实客户端交互仍由 9.4 人工验收。
