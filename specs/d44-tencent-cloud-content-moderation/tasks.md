# D44 Tasks：腾讯云内容安全审核执行清单

更新日期：2026-07-12

## D44 执行任务

- [x] D44.1 建立中文 D44 spec 三件套。
  - [x] `requirements.md` 描述图片、视频、文本审核、读取隔离、回调安全、重试、人工复核、部署和验证要求。
  - [x] `design.md` 描述数据模型、审核模块、腾讯云接入、媒体和文本流程、管理员接口、发布与测试设计。
  - [x] `tasks.md` 提供后续严格按范围执行的 Kiro 风格清单。

- [ ] D44.2 使用测试先行方式新增审核数据模型。
  - [ ] 先增加迁移检查，证明缺少审核任务表、文本提案表、审计表和媒体审核字段时检查失败。
  - [ ] 新增 `0024_content_moderation.sql`。
  - [ ] 创建 `content_moderation_jobs`，包含不可变版本唯一约束、队列索引、租约、结果和错误字段。
  - [ ] 创建 `content_moderation_text_proposals`。
  - [ ] 创建 `content_moderation_audit_logs`。
  - [ ] 给 `session_album_photos` 增加 `moderation_status` 和查询索引。
  - [ ] 确认现有媒体回填为 `approved_legacy`。
  - [ ] 确认所有新媒体创建路径显式写入 `pending`。
  - [ ] 运行迁移检查并确认通过。

- [ ] D44.3 使用测试先行方式实现内容审核基础模块。
  - [ ] 先编写状态常量、结果映射、文本标准化和摘要的失败测试。
  - [ ] 新增 `apps/api/src/modules/content-moderation/constants.js`。
  - [ ] 新增 `normalize.js`，统一映射 Pass、Review、Block。
  - [ ] 未知或畸形响应必须映射为 error，不能映射为 Pass。
  - [ ] 实现带字段标签的稳定文本标准化和 SHA-256 摘要。
  - [ ] 限制响应摘要大小并剔除密钥、令牌、签名 URL 和完整私密文本。
  - [ ] 运行单元测试并确认通过。

- [ ] D44.4 使用测试先行方式实现审核配置和腾讯云客户端适配器。
  - [ ] 先编写生产配置缺失时关闭式失败的测试。
  - [ ] 在 API 配置中增加总开关及文本、图片、视频分开开关。
  - [ ] 增加地域、CI 图片策略、CI 视频策略、TMS 策略、回调地址、回调令牌和重试上限配置。
  - [ ] 更新 `.env.production.example` 和配置检查脚本。
  - [ ] 新增可注入的 `tencent-client.js`，分别提供图片、视频、文本审核方法。
  - [ ] 确认客户端和日志不暴露 SecretKey。
  - [ ] 运行配置与客户端单元测试并确认通过。

- [ ] D44.5 使用测试先行方式实现审核任务仓储和状态机。
  - [ ] 先编写任务幂等创建、合法迁移、非法迁移和管理员决定优先级测试。
  - [ ] 实现审核任务创建、按 ID/服务商任务 ID/`data_id` 查询和行锁。
  - [ ] 实现 pending、processing、approved、review、rejected、error 状态迁移。
  - [ ] 实现文本提案创建、过期判断和条件应用。
  - [ ] 实现管理员操作审计记录。
  - [ ] 确认重复任务和重复终态结果幂等。
  - [ ] 运行仓储和状态机测试并确认通过。

- [ ] D44.6 使用测试先行方式改造图片为先审后发。
  - [ ] 先增加图片 finalize 后必须 pending 且无媒体 URL 的失败测试。
  - [ ] 修改图片最终记录创建逻辑，显式写 `moderation_status = pending`。
  - [ ] 按媒体 ID 和 ETag 幂等创建 CI 图片审核任务。
  - [ ] 提交失败时保留隐藏媒体并进入 error/retry。
  - [ ] Pass 时批准媒体，Review 时进入后台，Block 时创建一次清理任务。
  - [ ] 上传者只收到审核状态占位。
  - [ ] 运行图片定向测试并确认通过。

- [ ] D44.7 使用测试先行方式改造视频为画面和音轨先审后发。
  - [ ] 先增加视频 processing ready 不能绕过 moderation 的失败测试。
  - [ ] 修改视频创建路径，显式写 `moderation_status = pending`。
  - [ ] 源对象稳定后提交 CI 视频画面和音轨审核。
  - [ ] 保持转码回调只更新技术处理状态，不修改内容审核状态。
  - [ ] Pass、Review、Block 使用统一状态机。
  - [ ] Block 为源、展示和封面对象幂等创建清理任务。
  - [ ] 运行视频定向测试并确认通过。

- [ ] D44.8 使用测试先行方式封闭所有媒体读取路径。
  - [ ] 先为列表、分享、计数、标签、预览、下载、视频流和直连签名 URL 编写非 approved 拒绝测试。
  - [ ] 普通相册列表只返回 approved/approved_legacy。
  - [ ] 公开分享和媒体数量不包含未批准媒体。
  - [ ] 标签和批量下载候选不包含未批准媒体。
  - [ ] 图片预览、缩略图和下载接口独立校验审核状态。
  - [ ] 视频 range 流、封面和下载接口独立校验审核状态。
  - [ ] COS 直连签名 URL 只为 approved/approved_legacy 生成。
  - [ ] 上传者管理响应只返回无 URL 的状态占位。
  - [ ] 运行媒体访问门禁测试并确认通过。

- [ ] D44.9 使用测试先行方式接入用户文本 TMS 审核。
  - [ ] 先为昵称、评价、留言、置顶留言、私有门店/剧本和拼车说明编写 Pass/Review/Block/Error 测试。
  - [ ] 在 API 服务边界实现统一 `moderateTextMutation`。
  - [ ] 一次变更的多个短字段带标签合并送审，并按最高风险结论处理。
  - [ ] Pass 应用业务变更。
  - [ ] Block 返回稳定错误且不修改实体。
  - [ ] Review 保存隐藏提案且不修改现有线上值。
  - [ ] Error 保存隐藏提案并进入重试，绝不自动放行。
  - [ ] 系统生成文本、手机号和管理员审核备注不送审。
  - [ ] 运行文本审核定向测试并确认通过。

- [ ] D44.10 使用测试先行方式实现腾讯云安全回调。
  - [ ] 先编写未认证、超大请求体、未知结构、合法回调、重复回调和过期回调测试。
  - [ ] 新增 `POST /api/internal/content-moderation/tencent/callback`。
  - [ ] 校验腾讯云原生签名或高强度回调令牌。
  - [ ] 严格限制请求体大小和事件结构。
  - [ ] 匹配任务 ID、`data_id`、业务对象、对象 Key 和 ETag/摘要。
  - [ ] 使用事务行锁执行状态迁移。
  - [ ] 重复终态回调幂等返回成功。
  - [ ] 过期回调和管理员决定后的回调不得改变当前状态。
  - [ ] 运行回调安全测试并确认通过。

- [ ] D44.11 使用测试先行方式实现审核重试 Worker。
  - [ ] 先编写租约互斥、可重试错误、不可重试运营错误、退避和重试耗尽测试。
  - [ ] 新增审核重试任务认领仓储方法。
  - [ ] 使用 `FOR UPDATE SKIP LOCKED` 和租约令牌防止重复处理。
  - [ ] 网络、超时、限流和 5xx 使用带抖动的有限指数退避。
  - [ ] 认证、CAM、策略、额度和欠费错误保持隐藏并触发高优先级告警。
  - [ ] 超过重试上限后保留 error 并停止自动认领。
  - [ ] 新增 `job:content-moderation-retry` 命令和生产运行配置。
  - [ ] 运行 Worker 测试并确认通过。

- [ ] D44.12 使用测试先行方式实现管理员审核 API。
  - [ ] 先编写普通用户 403、队列筛选、详情、批准、拒绝和重试测试。
  - [ ] 新增管理员审核队列和详情接口。
  - [ ] 新增 approve、reject、retry 动作。
  - [ ] reject 必须填写原因。
  - [ ] approve 只应用当前不可变版本；文本基线变化时标记 stale。
  - [ ] retry 不改变内容可见性。
  - [ ] 所有动作写入审计记录。
  - [ ] 管理员决定优先于后续腾讯云回调。
  - [ ] 运行管理员 API 测试并确认通过。

- [ ] D44.13 实现管理后台最小人工复核页面。
  - [ ] 先增加管理后台 API wrapper 和运行时检查的失败测试。
  - [ ] 增加“内容审核”导航入口。
  - [ ] 增加 review/error 队列表格。
  - [ ] 支持内容类型、状态、标签和提交时间筛选。
  - [ ] 详情展示安全预览、提交者、关联实体、建议、标签、分数、重试次数和时间。
  - [ ] 提供批准、必填原因的拒绝和错误任务重试操作。
  - [ ] 管理员预览使用短时 URL。
  - [ ] 运行管理后台测试和构建并确认通过。

- [ ] D44.14 改造小程序审核状态体验。
  - [ ] 先增加小程序静态或单元检查，证明未实现审核状态提示时失败。
  - [ ] 相册上传完成后为上传者显示无媒体内容的审核状态占位。
  - [ ] pending/error 展示“内容正在审核”。
  - [ ] review 展示“内容需要进一步审核”。
  - [ ] rejected 展示“内容未通过安全审核，如有疑问请联系客服”。
  - [ ] 文本进入 Review 时保持当前页面数据不变并提示已进入审核。
  - [ ] 小程序不展示腾讯云标签、分数或敏感命中词。
  - [ ] 运行小程序测试和构建并确认通过。

- [ ] D44.15 增加指标、日志和运维告警。
  - [ ] 增加任务创建、提交成功/失败、回调延迟和 Pass/Review/Block 指标。
  - [ ] 增加回调认证失败、过期回调、重试耗尽和未批准访问拒绝指标。
  - [ ] 增加队列深度、最老任务年龄、结果比例和错误类型监控。
  - [ ] 对认证、CAM、额度、欠费和策略错误配置告警。
  - [ ] 对“COS 冻结但无业务任务”配置告警。
  - [ ] 确认指标标签和日志不含完整私密文本或签名 URL。

- [ ] D44.16 编写腾讯云控制台和生产发布手册。
  - [ ] 记录 CI/TMS 开通和审核策略配置步骤。
  - [ ] 记录图片、视频上传前缀的 COS 自动审核与冻结配置。
  - [ ] 记录图片色情、政治/违法、暴恐、广告/二维码、OCR 策略选择。
  - [ ] 记录视频画面和音轨策略选择。
  - [ ] 记录 CAM 最小权限、回调配置和密钥轮换要求。
  - [ ] 记录每日额度、费用和欠费告警配置。
  - [ ] 记录文本、图片、视频依次启用和观察指标。
  - [ ] 记录安全回滚：关闭新提交，不移除门禁，不批准待审内容。

- [ ] D44.17 增加 D44 自动检查和烟测。
  - [ ] 新增 `d44:unit`，覆盖结果映射、摘要、状态机、回调和重试。
  - [ ] 新增 `d44:check`，覆盖迁移、入口接入和读取门禁。
  - [ ] 新增 `d44:smoke`，覆盖图片、视频、文本和管理员复核主链路。
  - [ ] 将 D44 检查加入根 `npm run check`。
  - [ ] 确认测试使用假腾讯云客户端，不依赖公网和生产凭证。

- [ ] D44.18 执行本地完整验证。
  - [ ] 运行 `npm run d44:unit`。
  - [ ] 运行 `npm run d44:check`。
  - [ ] 运行 `npm run d44:smoke`。
  - [ ] 运行相册图片和视频现有测试。
  - [ ] 运行管理后台测试与构建。
  - [ ] 运行小程序媒体测试与构建。
  - [ ] 运行 `npm run check`。
  - [ ] 修复 D44 范围内导致的测试、检查或构建失败。

- [ ] D44.19 执行非生产环境真实腾讯云联调。
  - [ ] 使用腾讯云允许的正常样本验证 Pass。
  - [ ] 使用腾讯云允许的测试样本验证 Review。
  - [ ] 使用腾讯云允许的测试样本验证 Block 和 COS 冻结。
  - [ ] 验证回调认证、重复回调和过期回调。
  - [ ] 模拟超时、权限或额度错误，确认内容保持隐藏并告警。
  - [ ] 记录真实回调延迟、策略标识和联调结果，不记录敏感样本内容。

## D44 验收

- [x] D44 requirements 已落地到 [requirements.md](./requirements.md)。
- [x] D44 design 已落地到 [design.md](./design.md)。
- [x] D44 tasks 已落地到本文件。
- [ ] 上线后新图片在 Pass 或管理员批准前不可被其他用户读取。
- [ ] 上线后新视频画面和音轨在 Pass 或管理员批准前不可播放或下载。
- [ ] 所有列表、分享、计数、标签、预览、视频流、下载和直连 URL 都执行审核门禁。
- [ ] 昵称、评价、留言、置顶留言、门店/剧本和拼车说明按范围接入 TMS。
- [ ] Review 文本不覆盖现有已批准值。
- [ ] Block 文本不修改业务实体。
- [ ] 腾讯云异常、欠费、额度或权限错误不会自动发布内容。
- [ ] 回调经过认证且重复、过期回调不能改变有效终态。
- [ ] 管理员可以筛选、查看、批准、拒绝和重试审核项目，并保留审计。
- [ ] 现有媒体保持 `approved_legacy` 可用且未执行历史扫描。
- [ ] COS 自动审核与冻结、CAM 最小权限、额度和费用告警已按手册配置。
- [ ] 本地自动化验证全部通过。
- [ ] 非生产环境真实腾讯云 Pass、Review、Block、超时和重复回调联调通过。

## 验证记录

- D44 中文 spec 三件套已创建。
- 尚未开始业务实现，D44.2 至 D44.19 均保持未完成。

