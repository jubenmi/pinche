# D54 任务：公开相册全量分享与九图封面

- [x] 1. 建立规格与隔离基线
  - [x] 1.1 从 `develop` 创建隔离工作树并安装锁定依赖、初始化子模块。
  - [x] 1.2 运行 `npm run check` 验证基线。
  - [x] 1.3 创建 D54 `requirements.md`、`design.md`、`tasks.md` 和实施计划。

- [ ] 2. 移除正文照片数量上限，保留九图封面与三视频限制
  - [ ] 2.1 先写 31/100 张默认与自定义分享的失败测试。
  - [ ] 2.2 使正文快照、摘要、未标注快照和选择器接受完整静态照片集合。
  - [ ] 2.3 保持封面 ID 最多 9、视频最多 3，并验证旧 30 项摘要兼容。

- [ ] 3. 实现服务端签名游标与有界读取
  - [ ] 3.1 先写首/中/末页、失效项填补、篡改及跨分享游标失败测试。
  - [ ] 3.2 实现签名游标、按快照 offset 扫描和每段最多 30 ID 的动态复核。
  - [ ] 3.3 扩展公共相册路由的 `cursor`、`limit`、`next_cursor` 与 `has_more` 契约。

- [ ] 4. 实现小程序追加加载
  - [ ] 4.1 先写分页 URL、去重合并、错误保留和过期响应关闭的失败测试。
  - [ ] 4.2 新增纯分页 helper，并在 `album.vue` 加入首屏重置、触底加载和局部重试状态。
  - [ ] 4.3 确认封面准备和原生分享菜单不等待完整正文。

- [ ] 5. 建立 D54 门禁并验证发布质量
  - [ ] 5.1 新增 D54 API、小程序和静态契约检查，接入 `package.json`。
  - [ ] 5.2 运行 D54 聚焦测试、D48/D50/D52 回归、小程序构建、全量 `npm run check` 和 `git diff --check`。
  - [ ] 5.3 逐项回填本任务清单的自动验证证据。
