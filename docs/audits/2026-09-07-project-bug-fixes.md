# 全项目 Bug 扫描与修复记录（2026-09-07）

本轮覆盖 API、管理端、小程序、共享时间工具和 Talk 子模块，重点检查北京时间/UTC 边界、开本时刻、过期时间、异步请求竞态、访问权限和构建检查。下列问题均通过代码路径及可复现测试确认；本记录不表示项目中不再存在未知问题。

本文下方记录的是原扫描工作区（基于 `b38c79a0`）的修复和测试证据。发布整合以更新的 `origin/develop`（`30bddd3022d49a176adafb3966464794f71407e2`）为基线：保留新版历史补录、服务端分享能力与已存在的时间修复，只补入仍缺少的防护。新版已使用并压缩 `photo-claim-share.jpg`，因此发布版本保留其静态资源位置。发布阶段的测试与分支结果单独记录，不能用本节的旧工作区结果替代。

## 已修复的问题

| 范围 | 复现与影响 | 修复结果 |
| --- | --- | --- |
| 建车时间入库 | 两个客户端提交北京时间文本，API 原样写入 UTC DATETIME，19:30 可能变成次日 03:30 | 新客户端发显式 UTC；API 将旧客户端北京时间文本规范化为 Date，再交由 UTC MySQL 连接写入 |
| 建车时间合法性 | 非法日历日期、错误时区偏移、已过时间未统一阻止 | 共享解析器严格校验；API 以数据库当前时间判断；前端提交前给出可操作提示 |
| 时间选择器 | 纯时分选择器缺少明确边界，在晚上选择未来日期的上午可能被截断 | 固定完整 00:00–23:59 范围，日期另行选择 |
| 创建草稿日期 | 默认日期依赖设备时区；UTC 草稿直接截字符串恢复，日期和时分偏移 | 默认日期、草稿恢复和日期下限统一按北京时间处理 |
| 订阅通知 | 报名通知直接 String(Date)，可能得到英文星期/错误日期字段 | 使用明确的北京时间格式生成微信 date 字段 |
| 页面与聊天时间 | 相册依赖本地时区；待审消息暴露 ISO；聊天及默认置顶截取 UTC | 所有这些展示路径使用共享北京时间解析和格式化 |
| 小程序版本时间 | UTC 构建机生成的版本日期与管理端的北京时间标签不一致，跨日时差一天 | 构建配置使用共享北京时间格式化，并补上跨日回归 |
| 页面跨时刻刷新 | Vue computed 直接读 Date.now，跨午夜、开本时刻后仍缓存旧结果 | 日历、管理、详情、分享和管理端预览使用响应式时钟；隐藏暂停、返回校准、卸载永久销毁 |
| 业务时钟不一致 | 改期、访客详情、评价审核预检使用应用服务器时间，与数据库开放条件不一致 | 边界统一依据数据库时间，加入应用时钟超前/落后的回归 |
| 发布草稿 | 过期草稿可发布；取消/锁定状态可被重新招募；转移车头时存在权限竞态 | 锁定会话后验证当前车头、状态、时间；最终 UPDATE 再核验开本截止条件 |
| 建车内容审核 | 审核通过时开本时间已过，时间验证异常可能使任务反复失败 | 入队前预检；审核后重新验证；失效建车提案进入 stale 终态并释放租约。其他动作的普通输入错误保持原语义 |
| 登录票据与凭证 | 已批准扫码票据绕过过期检查；部分 token 在到期那一秒仍有效 | pending/approved 票据均过期；业务、相册、媒体、分享及邀请 token 使用严格截止时间；拒绝畸形 exp |
| 管理端登录过期 | 当前凭证 401 后继续显示登录态；旧请求可能影响新登录 | 当前凭证 401 清理存储并通知根视图；覆盖 JSON、表单和同源媒体；旧凭证失败不能注销新登录，COS 错误不误登出 |
| 扫码登录轮询 | 刷新二维码或卸载后，旧响应仍可能写回或重复轮询 | 每代请求隔离、单次在途请求、终态清理和卸载保护 |
| 聊天记录窗口 | 升序 LIMIT 100 永远返回最老 100 条 | 取最新 100 条，再按时间及 ID 顺序展示 |
| 聊天生命周期 | 慢请求、切车、隐藏和返回时可重复轮询/串写旧结果；取消草稿及登录结果也可污染新车状态 | 两份 ChatEntry 同步修复，请求绑定会话和代次；隐藏停止、返回恢复，保留编辑草稿；卸载后不复活 |
| NPC 重进权限 | 被禁止重进的用户可从 NPC 认领绕过普通座位权限 | NPC direct/review_required 均在写入前校验重进限制 |
| NPC 创建返回值 | 新建排序靠前的 NPC 时，接口返回列表末尾的旧角色 | 根据数据库 insertId 返回实际新角色 |
| 小程序包体积 | 未使用的 287.5 KB 设计图被 static 自动打包，导致单文件及主包预算失败 | 原样迁入 docs/design-assets，保留已使用的分享图；重新构建并执行体积检查 |
| 检查遗漏 | 根检查未执行完整管理端检查，新回归也缺少统一入口 | 根检查执行管理端 check，并通过 postcheck 运行所有 API/小程序/管理端/shared/Talk 测试 |

## 验证方式

- 关键修复先加入针对实际函数/组件脚本的回归，观察失败，再完成修复并确认通过。数据库边界测试使用受控连接；创建时间测试额外调用实际 mysql2 日期转义验证 UTC 写入值。
- `npm run check` 包含既有 D 系列检查、语法和安全边界检查、小程序包检查，以及完整 `test:unit`。
- 生产构建：`npm run build:admin-web`、`npm run build:mp-weixin`。小程序构建仅报告既有 Sass 弃用提示。
- 时间专项分别在 `TZ=UTC`、`TZ=Asia/Shanghai`、`TZ=America/Los_Angeles` 下执行。
- 根仓库及 Talk 子模块分别执行 `git diff --check`；独立复核后修正了异步取消/登录、时钟卸载和发布权限竞态。

## 最终完成记录

| 检查 | 结果 | 本机日志 |
| --- | --- | --- |
| `npm run check`（含 precheck、check、postcheck） | exit 0；最终全量单元测试 1148/1148，无失败、跳过或取消 | `/tmp/pinche-check-final.log` |
| 管理端生产构建 | exit 0；91 个模块 | `/tmp/pinche-admin-build-final.log` |
| 小程序生产构建 | 完整检查内重新构建，`DONE Build complete` | `/tmp/pinche-check-final.log` |
| 时间专项：UTC | 65/65 | `/tmp/pinche-final-time-UTC.log` |
| 时间专项：Asia/Shanghai | 65/65 | `/tmp/pinche-final-time-Asia-Shanghai.log` |
| 时间专项：America/Los_Angeles | 65/65 | `/tmp/pinche-final-time-America-Los_Angeles.log` |
| 根仓库与 Talk 子模块 `git diff --check` | 均通过 | 命令输出为空，exit 0 |
| 小程序包体积 | 主包 1,519,177 字节（1483.6 KB），低于 1,572,864 字节预算；14 页面检查通过 | 同完整检查日志 |

新增 `npm run test:unit` 是后续重跑全量回归的统一入口。首次依赖安装完成后，监听本机端口的 HTTP 回归需要运行环境允许 localhost 绑定；本轮在获得工具批准后执行成功。

## 交付边界

- 当前机器没有可用 Docker/MySQL 服务，本轮未执行真实数据库集成、真机微信通知、线上审核 provider 或生产端到端验证。受控数据库测试不能替代这些环境验收。
- 修复面向后续写入和读取逻辑，没有批量平移历史 start_at。历史数据可能混有人工录入、旧客户端输入，必须基于来源逐条核对后才能回填。
- 未提交、推送或部署。`packages/talk` 是 Git 子模块，其修复和新增测试在子模块工作区，提交时需要单独处理。
- 原有 `package-lock.json`、D48 任务文件及其他用户资料改动保留。安装依赖使用 `--package-lock=false`。
- `photo-claim-share.jpg` 仅移动位置，SHA-256 保持 `ba05f973c0942fcf05dbb84986a924404795ad00162720d06b31842432745f65`。
- Grok 审查工具调用被拒绝后未重试，本轮使用本地测试及独立子任务复核。

## 同日补查：登录切换与重复发送

用户追问是否还有问题后，补查确认以下遗漏，已完成针对性修复：

| 问题 | 影响与修复 |
| --- | --- |
| 小程序旧请求 401 注销新登录 | JSON、文件和二进制上传绑定请求时的 token、后端地址和登录代次；匿名或旧请求不再清理当前身份，当前身份的 401 仍正常失效 |
| 旧身份资料和自动登录结果写回 | 资料、手机号、刷新身份及微信登录都拒绝失效代次的结果；ensureLoggedIn 区分“本次凭证过期”和“用户已经退出/切换”，保留正常续登并停止旧登录流程 |
| 非 JSON 上传 401 漏注销 | 在解析上传响应前处理 HTTP 401，避免代理 HTML 错误页绕过身份失效处理 |
| 聊天重复发送、等待期间编辑丢失 | 发送前锁定一次操作，固定点击时的文本和替换 ID；旧发送结果不再清空后续输入或误删后来编辑的其他草稿 |
| 审核通过后仍携带旧草稿 ID | 刷新聊天列表时解除已经离开私有草稿列表的替换 ID，下一条消息按正常新消息发送；保留仍存在的编辑草稿 |

补查回归包含小程序真实 API 模块的受控 UniApp 回调、两份真实聊天组件脚本，以及登录/审核前后的完整异步序列。首轮 API 用例出现 13 个失败；独立复核新增登录包装流程，再复现 5 个失败。各问题经修复后定向测试 95/95 通过，其中身份 27 项、Talk 68 项。

补查最终 `npm run check` exit 0，全量 **1195/1195** 通过，未跳过用例；检查内的小程序生产构建成功，日志 `/tmp/pinche-followup-check-final.log`。管理端源码未在补查中更改，沿用前一轮通过的生产构建。
