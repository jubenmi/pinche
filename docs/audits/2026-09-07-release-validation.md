# 0.20260907.1102 发布前验证

发布基于 `origin/develop` 的 `30bddd3022d49a176adafb3966464794f71407e2`，将旧扫描工作区的修复按当前实现整合。保留 D51 模块结构、剧本谜品牌、历史补录、分享能力、相册分页和历史车局发布规则。

版本号按用户要求使用北京时间 `0.YYYYMMDD.HHmm`。小程序 package、manifest 和 lockfile 均为 `0.20260907.1102`。上传窗口曾读取线上版本 `0.20260802.1443`。

## 修复与兼容性

- 创建、改期、发布、评价预检和车局详情的开本状态使用数据库时钟。普通车局到期后不能发布；历史补录保留原有合法过去时间和原子发布流程。
- 车局详情的 `has_started` 从保留数据库投影的行生成，避免 API 主机时钟偏差导致页面提前切换状态。
- 补齐 Token 截止边界、二维码轮询、小程序身份切换、聊天轮询和发送草稿竞态保护。
- Talk 子模块为 `91c9be7c28ff4c90b55a66d3cd384e2415f0f492`，已经推送 `codex/bug-audit-release-20260907`，保留历史置顶和现有业务时间格式化。
- 原有 31 张相册选择失败断言已按 D54 更新，实际验证 31/100 张完整快照和非法参数拒绝。审核截止测试使用可控时钟，验证读取响应体仍遵守同一个 5 毫秒截止时间。

## 本地验证

| 检查 | 结果 |
| --- | --- |
| Node 24 完整 `npm run check`，含 postcheck | exit 0；日志 `/tmp/pinche-release-final-check.log` |
| Node 24 全 API 单元测试 | 1314 通过，0 失败，1 项专用历史 MySQL 并发测试跳过；日志 `/tmp/pinche-release-api-unit-final.log` |
| 管理后台全目录测试 | 63/63 |
| 小程序全目录测试 | 378/378 |
| Shared 全目录测试 | 32/32 |
| Talk 全目录测试 | 73/73 |
| UTC、Asia/Shanghai、America/Los_Angeles 时间专项 | 各 39/39 |
| 管理后台、小程序生产构建 | 均通过；小程序有既存 Sass 弃用提示 |
| 生产只读发布预检 | `uploadReady: true`；API、数据库、店家和剧本接口通过 |

本机 Docker 不可用，因此真实 MySQL 验证由 CI 执行。CI 在原有 MySQL 8.4 集成之后新增 `test:business-time-mysql`，验证北京时间经 UTC DATETIME 存储后的往返结果；两项通过后才允许构建和推送镜像。

没有批量修改历史时间数据。原工作区的未提交 lockfile、D48 任务文件及用户资料保留；本次发布在独立工作区完成。本文记录发布前证据，分支 CI、镜像发布和微信上传结果以随后对应运行记录为准。
